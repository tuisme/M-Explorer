<?xml version="1.0" encoding="utf-8"?>
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorPastelGreen">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPastelGreen" />
            <corners android:radius="45dp" />
        </shape>
    </item>

    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:startColor="@color/colorPLight"
                android:endColor="@color/colorPastelGreen"
                android:type="linear" />
            <corners android:radius="45dp" />
        </shape>
    </item>
</ripple>